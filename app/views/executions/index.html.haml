.main
  #upload
    Please upload your trade executions file
    = form_for(@ef, :url => upload_executions_path, :html => {:multipart => true}) do |f|
      = f.file_field :file
      %br
      = f.submit "Upload Executions"
  .executions
    %h2 Your Past Executions
    - @executions.each do |e|
      %ul
        %li
          = link_to(e.date.to_s, execution_path(e.id))
    
  
    